{
  "rules": {
    ".read": false,
    ".write": false,
    "Image": {
      ".read": "auth != null && (root.child('Whitelist').hasChild(auth.token.email.replace('.', '_')) || root.child('Whitelist').hasChild(auth.uid.replace('.', '_')))",
      ".write": true
    },
    "DoorState": {
      ".read": "auth != null && (root.child('Whitelist').hasChild(auth.token.email.replace('.', '_')) || root.child('Whitelist').hasChild(auth.uid.replace('.', '_')))",
      ".write": true
    },
    "DoorAlert": {
      ".read": "auth != null && (root.child('Whitelist').hasChild(auth.token.email.replace('.', '_')) || root.child('Whitelist').hasChild(auth.uid.replace('.', '_')))",
      ".write": true
    },
    "Whitelist": {
      ".write": false,
      ".read": true
    }
  }
}
